# Environments
.venv/
venv/
env/

# Secrets — never commit real .env files, only .env.example
.env
*.env
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
build/
dist/

# Test artifacts — pytest coverage output, and any stray local db files
# (our tests run against real Postgres per PLAN.md, but this guards against
# an accidental sqlite file from a misconfigured test run)
.coverage
.coverage.*
htmlcov/
*.db
*.sqlite3

# OS cruft
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/

# Personal Claude Code instructions — not shared with the team
CLAUDE.local.md
